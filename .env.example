# CDP (Coinbase Developer Platform) API Credentials
# Get these from https://portal.cdp.coinbase.com/
#
# IMPORTANT: Your CDP API Key must have the following scopes enabled:
# - accounts#create (to create new wallet accounts)
# - accounts#export (to export private keys)
# - accounts#read (to read account information)
#
# Configure these scopes in the CDP Portal when creating your API key
CDP_PROJECT_ID=your-project-id
CDP_API_KEY_ID=your-api-key-id
CDP_API_KEY_SECRET=your-api-key-secret
CDP_WALLET_SECRET=your-wallet-secret

# API URL - Update this with your deployed Vercel URL
NEXT_PUBLIC_API_URL=http://localhost:3000
